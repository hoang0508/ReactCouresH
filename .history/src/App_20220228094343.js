import React from "react";
import Modal from "./components/Modal/Modal";

const App = () => {
  return (
    <div>
      <Modal open={true} handleClose={() => {}} />
    </div>
  );
};

export default App;
